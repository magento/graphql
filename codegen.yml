overwrite: true
schema:
    './src/**/*.ts':
        noRequire: true
generates:
    generated/graphql.d.ts:
        plugins:
            - 'typescript'
            - 'typescript-resolvers'
        config:
            useIndexSignature: true
