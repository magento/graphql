overwrite: true
schema:
    - './src/**/*.ts':
          noRequire: true
    - '!**/__tests__/*'
generates:
    generated/graphql.d.ts:
        plugins:
            - 'typescript'
            - 'typescript-resolvers'
        config:
            scalars:
                ID: 'string | number'
